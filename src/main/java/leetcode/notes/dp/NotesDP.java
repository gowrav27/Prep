package leetcode.notes.dp;

public class NotesDP
{
    /*
     * UniqueBinarySearchTrees = result[i] = result[i] + result[k - 1] * result[i - k];
     */

    /**
     * Longest Increasing Subsequence <br>
     * if(nums[i]>nums[j])<br>
     * {<br>
     * max[i]=Math.max(max[i], max[j]+1); <br>
     * }
     */
}
